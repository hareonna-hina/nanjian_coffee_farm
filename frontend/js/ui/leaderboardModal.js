/** 排行榜弹窗模块：把 leaderboard 聚合结果渲染为名次表，并高亮当前玩家。 */
(function(CG){
  CG.ui=CG.ui||{};
  /** 打开排行榜；异步等待公共 view，onClose 用于从终局成绩卡查看后返回原卡。 */
  CG.ui.showLeaderboard=async function(onClose){const data=await CG.leaderboard.get(),profile=CG.profile.getOrCreate(),mine=data.rows.find(r=>r.uid===profile.publicUid),safe=CG.format.html;CG.ui.openModal(`<article class="leaderboard-card" role="dialog" aria-modal="true" aria-label="排行榜"><button class="modal-close" type="button" data-close aria-label="返回">×</button><div class="leaderboard-list modal-scroll">${data.rows.length?data.rows.map(r=>`<div class="leaderboard-row ${r.uid===profile.publicUid?"is-me":""}"><strong>${r.rank}</strong><span>${safe(r.name)}<small style="display:block;font-size:7px;color:#756d58">${safe(r.uid)}</small></span><b>${CG.format.money(r.profit)}</b><span>${r.sca.toFixed(1)}</span></div>`).join(""):`<div class="my-rank-empty">排行榜暂无已完成成绩</div>`}</div><div class="my-rank">${mine?`<div class="leaderboard-row is-me"><strong>${mine.rank}</strong><span>${safe(mine.name)}<small style="display:block;font-size:7px;color:#756d58">${safe(mine.uid)}</small></span><b>${CG.format.money(mine.profit)}</b><span>${mine.sca.toFixed(1)}</span></div>`:`<div class="my-rank-empty">未完成</div>`}<small class="my-rank-note">每位庄园主只保留净利润最高的一局。${data.source==="local"?" 当前为本地榜单。":""}</small></div></article>`,true,onClose)};
})(window.CoffeeGame);
